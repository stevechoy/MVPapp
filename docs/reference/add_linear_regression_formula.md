# Add Linear Regression Formula (not currently used)

A function that adds linear regression formula and place it as a text
annotation on the top of a plot. Requires ggplot object as input.

## Usage

``` r
add_linear_regression_formula(p)
```

## Arguments

- p:

  ggplot object

## Value

A ggplot object with the formula placed on the top
