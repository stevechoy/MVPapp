# A safe version of shiny incProgress, mostly for used in testing

A safe version of shiny incProgress, mostly for used in testing

## Usage

``` r
safely_incProgress(amount, detail = NULL)
```

## Arguments

- ...:

  args to be passed

## Value

A shiny withProgress bar or print to console

## See also

`parse`
