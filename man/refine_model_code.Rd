% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{refine_model_code}
\alias{refine_model_code}
\title{Provide a model code string and its compile error message to be refined}
\usage{
refine_model_code(
  model_code,
  error_message,
  context = NULL,
  reuse_context = FALSE,
  service,
  api_key,
  api_upload = NULL,
  api_chat = NULL,
  user_id = "mrgsolve_translator",
  model_gemini = "gemini-3.6-flash",
  model_openai = "gpt-5-mini",
  model_anthropic = "claude-haiku-4-5-20251001",
  model_openrouter = "arcee-ai/trinity-large-preview:free",
  model_openai_compatible = "gpt-5-mini",
  model_deepseek = "deepseek-reasoner",
  model_apollo = "gpt-5.6-terra",
  model_azure = "gpt-5.6-terra",
  model_aws = "anthropic.claude-sonnet-5",
  progress_bar = 0.4,
  max_retries = 2,
  display_info = TRUE,
  temperature = 0,
  seed = 42,
  attempt = 1,
  deep_pdfscan = FALSE,
  force_parse = FALSE,
  system_prompt,
  long_user_prompt,
  short_user_prompt,
  internal_version,
  feedback_success = FALSE,
  max_tokens = 128000,
  debug = TRUE
)
}
\arguments{
\item{model_code}{Input model code}

\item{error_message}{Input compilation error message}

\item{context}{Argument to hold chat_obj or conversation_id to keep same chat session}

\item{reuse_context}{Set to TRUE to re-use same conversation_id or chat_obj, more costly in terms of time and tokens}

\item{service}{Choice of "PROD", "EXP", "Gemini", "OpenAI", "Claude", "OpenRouter",
"OpenAI-Compatible", "DeepSeek", "Apollo", "Azure OpenAI", "AWS Bedrock"}

\item{api_key}{API Key, recommended to store it as env var called "ANTHROPIC_API_KEY" etc (Dify only)}

\item{api_upload}{API URL for uploading of files (Dify requires a 2-step process)}

\item{api_chat}{API URL for chat messages, required for OpenAI-compatible}

\item{user_id}{user id for the request}

\item{model_gemini}{Model to be used when calling Gemini API}

\item{model_openai}{Model to be used when calling OpenAI API}

\item{model_anthropic}{Model to be used when calling Anthropic API}

\item{model_openrouter}{Model to be used when calling OpenRouter}

\item{model_openai_compatible}{Model to be used when calling OpenAI-compatible API}

\item{model_deepseek}{Model to be used when calling DeepSeek}

\item{model_apollo}{Model to be used when calling Apollo LLM}

\item{model_azure}{Model to be used when calling Azure OpenAI}

\item{model_aws}{Model to be used when calling AWS Bedrock}

\item{progress_bar}{Starting point of Shiny progress bar, relevant for 2nd try}

\item{max_retries}{Required to calculate a more accurate progress bar}

\item{display_info}{Set to TRUE to show how much time/tokens in Shiny UI when job is finished}

\item{temperature}{Goes from 0 to 1, where 0 is deterministic, if not reusing context}

\item{seed}{seed number for LLMs, if not reusing context}

\item{attempt}{current attempt number}

\item{deep_pdfscan}{Uses Vision to extract image data (BI only)}

\item{force_parse}{Force reparsing (BI only)}

\item{system_prompt}{Character. System prompt}

\item{long_user_prompt}{Character. Long user prompt}

\item{short_user_prompt}{Character. Short user prompt}

\item{internal_version}{Logical. Only relevant for BI}

\item{feedback_success}{Logical. Only relevant for BI, and if reuse_context = TRUE}

\item{max_tokens}{Numeric. Maximum output tokens.}

\item{debug}{Displays debug messages}
}
\value{
a named list of "answer", "conversation_id", "chat_obj"
}
\description{
Sends a string containing model code and error message to a Dify Workflow or
LLM API for correction.
}
