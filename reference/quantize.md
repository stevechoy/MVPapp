# Used for dividing bins for median line plots

Used for dividing bins for median line plots

## Usage

``` r
quantize(x, levels, ...)
```

## Arguments

- x:

  vector of values (e.g. x-axis)

- levels:

  bins

- ...:

  extra params to pass through to cut()
